server:
  port: 30001
spring:
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: false
  datasource:
    primary:
      url: jdbc:postgresql://${primary.host}:${primary.port}/data-garden
      username: ${primary.username}
      password: ${primary.password}
      default_schema: lanzhou
      show-sql: true
      ddl-auto: update
    doris:
      url: jdbc:mysql://${doris.host}:${doris.port}
      username: ${doris.username}
      password: ${doris.password}
    dolphin-scheduler:
      url: jdbc:mysql://${dolphin-scheduler.host}:${dolphin-scheduler.port}/dolphinscheduler
      username: ${dolphin-scheduler.username}
      password: ${dolphin-scheduler.password}

---
primary:
  host: localhost
  port: 5432
  username: cryptoneedle
  password: postgres
doris:
  enabled: true
  host: 10.112.206.62
  port: 9030
  username: root
  password: Seatone@2024#$
dolphin-scheduler:
  enabled: false
  host: 10.131.50.117
  port: 3306
  username: admin
  password: dolphinscheduler123